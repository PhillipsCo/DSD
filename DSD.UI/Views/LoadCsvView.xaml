<UserControl x:Class="DSD.UI.Views.LoadCsvView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DSD.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
               Text="CSV Folder:"
               FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
               TextWrapping="Wrap"
               FontSize="13"
               Margin="0,0,0,12">
        When you press Send, the data will be inserted into the database for the selected customer.
        Make sure all CSV files are renamed with a date so they can be properly organized in the archive.
        </TextBlock>

        <Grid Grid.Row="2"
          Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                Content="Browse..."
                Width="90"
                Height="30"
                VerticalAlignment="Center"
                Margin="0,0,8,0"
                Click="Browse_Click"/>

            <TextBox Grid.Column="1"
                 x:Name="FilePathTextBox"
                 VerticalContentAlignment="Center"
                 IsReadOnly="True"
                 MinWidth="300"/>

            <Button Grid.Column="2"
                Content="Send"
                Width="90"
                Height="30"
                VerticalAlignment="Center"
                Margin="8,0,0,0"
                Click="Send_Click"/>
        </Grid>

        <TextBlock x:Name="StatusTextBlock"
               Grid.Row="3"
               Margin="0,12,0,0"
               TextWrapping="Wrap"
               Foreground="DarkSlateGray"/>

    </Grid>

</UserControl>
