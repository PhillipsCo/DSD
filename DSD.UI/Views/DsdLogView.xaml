<UserControl x:Class="DSD.UI.Views.DsdLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DSD.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="View Date:"
                   VerticalAlignment="Center"
                   Margin="0,0,8,0"/>


            <DatePicker
    SelectedDate="{Binding SelectedDate}"
    SelectedDateChanged="DatePicker_SelectedDateChanged"
    Width="140"/>

        </StackPanel>

        <DataGrid Grid.Row="1"
          ItemsSource="{Binding Logs}"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          CanUserAddRows="False"
          AlternatingRowBackground="#EEF2F7">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Customer"
                            Binding="{Binding Cust}" />

                <DataGridTextColumn Header="Job"
                            Binding="{Binding Job}" />

                <DataGridTextColumn Header="Target"
                            Binding="{Binding TargetComputer}" />

                <DataGridTextColumn Header="Schedule Date"
                            Binding="{Binding ScheduleDate}" />

                <DataGridTextColumn Header="Schedule Time"
                            Binding="{Binding ScheduleTime}" />

                <DataGridTextColumn Header="Start"
                            Binding="{Binding StartDate}" />

                <DataGridTextColumn Header="End"
                            Binding="{Binding EndDate}" />

                <DataGridTextColumn Header="Status"
                            Binding="{Binding Status}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>

</UserControl>
