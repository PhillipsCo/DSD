<UserControl x:Class="DSD.UI.Views.ApiTesterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Get Token Button -->
        <Button Grid.Row="0"
                Content="Get Access Token"
                Width="160"
                Margin="0,0,0,12"
                Command="{Binding GetTokenCommand}"
                 />

        <!-- Expires -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Expires:"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>


            <TextBox
    Width="220"
    IsReadOnly="True"
    Text="{Binding ExpiresAtDisplay,Mode=OneWay}" />

        </StackPanel>

        <!-- Access Token (long) -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Access Token:" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Token.access_token}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     IsReadOnly="True"/>
        </StackPanel>
    </Grid>
</UserControl>
